



#include <stdio.h>

int main() {

  int x = 5;
  float y = 2;
  int z = 3;
  int j = 8;

  float w = x / y;
  float k = z / w;

  float r = x / 4.0;
  float b = (float)j / x;

  x = x + 1;
  x++;

  x = x - 1;
  x--;

  printf("A divisao de %d por %.2f e igual a %.2f\n", x, y, w);










  return 0;
}

#include <stdio.h>

int main(int argc, char const *argv[]) {

    
    int x, y;
    // int y;

    printf("Digite dois numero: ");
    scanf("%d", &x);
    scanf("%d", &y);
    printf("Voce digitou: %d e %d\n", x, y);

    return 0;
}
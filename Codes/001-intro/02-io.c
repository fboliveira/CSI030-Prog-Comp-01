#include <stdio.h>

int main() {

    int x = 5, y, w;
    printf("Ola,\tmundo!\n");
    printf("Digite um numero: ");
    scanf("%d", &y);
    // scanf("%d", &y);

    printf("X + Y = %d\n", w);

    w = x + y;

    return 0;
}